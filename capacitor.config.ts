import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.BancoConta.app',
  appName: 'BancoConta',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
